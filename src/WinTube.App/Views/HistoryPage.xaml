<Page
    x:Class="WinTube.App.Views.HistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:WinTube.App.Controls"
    xmlns:local="using:WinTube.App.Views"
    NavigationCacheMode="Required">
    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <GridView x:Name="CardsGrid" ItemsSource="{x:Bind cards}"
                      SelectionMode="None" IsItemClickEnabled="False"
                      Padding="24">
                <GridView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsWrapGrid Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </GridView.ItemsPanel>
                <GridView.ItemTemplate>
                    <DataTemplate x:DataType="local:VideoCardViewModel">
                        <controls:VideoCard Video="{x:Bind Video}"
                            ProgressFraction="{x:Bind ProgressFraction, Mode=OneWay}"
                            Clicked="OnVideoCardClicked"
                            ChannelClicked="OnChannelClicked"/>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>
        </ScrollViewer>

        <TextBlock x:Name="EmptyText" Text="No watch history yet."
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Style="{StaticResource SubtitleTextBlockStyle}"
                   Visibility="Collapsed"/>
    </Grid>
</Page>
